---
// src/components/Card.astro

// src/components/Card.astro
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class={`rounded-xl shadow-sm ${className || ''}`}>
  <slot />
</div>